// Seed script is no longer used — startup data now comes from YC API.
// Run `pnpm db:sync-yc` to populate the Company table with ~5,690 YC companies.
console.log("Use `pnpm db:sync-yc` to sync YC company data instead.");
